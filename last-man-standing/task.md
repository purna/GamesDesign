# Tasks

- [x] Split JavaScript into separate ES modules/files
- [x] Create `config.js` for shared static data
- [x] 1 Create game lifecycle state machine (WAITING → LOBBY → IN_GAME → GAME_OVER)
- [x] 2 Implement timer pause during active games
- [x] 3 Add "Game In Progress" waiting screen for new arrivals
- [x] 4 Build rich tile-based room renderer with baked canvas
  - [x] 4.1 Floor tiles with procedural noise dithering
  - [x] 4.2 Wall tiles with mortar lines and edge blending
  - [x] 4.3 Door tiles with wooden plank pattern
  - [x] 4.4 Pillar tiles with stone masonry
  - [x] 4.5 Decorative elements (torch particles, ground details)
  - [x] 4.6 Room-type theming (corner/interior/spawn variants)
- [x] 5 Upgrade player sprites to humanoid style
- [x] 6 Upgrade enemy sprites to armored soldier style
- [x] 7 Add torch/ambient particle effects
- [x] 8 Wire up state broadcasting over network
  - [ ] 9 Test multi-tab gameplay flow (needs two real browser tabs — see VERIFICATION.md)
  - [x] 10 Add podium screen with restart-to-lobby modal after game over
  - [x] 11 Display the current player's name at the top of the game screen
  - [x] 12 Add map-tile pickup that activates a 10-second minimap showing all players
  - [x] 13 Add a torch light to the players so we have a vignette effect to the scene.
  - [x] 14 Select a new host for each game
  - [x] 15 Restart to the start page or lobby after a winner
  - [x] 15.1 Add the player's name to the game screen
  - [x] 15.2 Put the health bar on its own row
  - [x] 15.3 Move inventory into a right-side canvas overlay and highlight it when items are held
  - [x] 15.4 Hide the room info text
  - [x] 15.5 Show the player's name and players-left count in the room timer
- [x] 16 use [](https://www.profanity.dev/#api) or https://www.purgomalum.com/ to check for appropriate usernames. 
  - [x] 16.1. limit length of username to 10 characters. (letters a-z plus -)
  - [x] 17 replace trophy with an SVG cup icon
  - [x] 18 Disable "Start Now" until at least 2 players have joined the lobby

## Cross-cutting stabilization

- [x] GAME_OVER reconciled: kept as a legacy wire value only, mapped to PODIUM on receipt
- [x] `startGame()` enforces MIN_PLAYERS_TO_START, not just the disabled button
- [x] `#room-info` retained as a visually hidden screen-reader/debug source
- [x] Door (2) and pillar (3) tile ids are now generated by `room.js`, not just drawn
- [x] Stale state removed (`inLobbyPhase`, `mapSprite`, `nameLabels`); `hostId`, `lastHostId`, `roundId`, `failoverSeq` and `mapActiveUntil` are now in use
- [x] `node verify.mjs` runs syntax, import, DOM-id and unit checks

## UI and stability pass

- [x] Game screen fits the viewport — no page scrolling; controls overlap the canvas only as far as a short window requires
- [x] Weaker torch light, outer edge at 90% darkness
- [x] Stroke icons for attack, defend and the map (controls and inventory)
- [x] Doors given their own material and colour, clearly distinct from walls
- [x] Extra floor decoration: stains, puddles, litter, weeds, broken slabs, more moss
- [x] Late arrivals spectate instead of joining a live match
- [x] Duplicate hosts resolved deterministically; rotation via nomination, then election
- [x] Map overlay cleared on death, spectating, round start and round end

## Network resilience and heading font

- [x] Expanded RELAY_URLS to 6 public Nostr relays (Trystero uses all of them in parallel, so one being rate-limited/banned no longer stalls matchmaking)
- [x] Added `requestAutoReconnect()` cooldown gate in game.js; every automatic reconnect site (arena rotation, empty-lobby timeout, podium -> lobby) now routes through it, so a stuck same-bucket condition can no longer reopen fresh relay connections faster than RECONNECT_COOLDOWN_MS
- [x] User-initiated join (Enter Arena) is untouched — only the app's own automatic reconnects are throttled
- [x] Black Ops One heading font wired in (preconnect + stylesheet link), applied to every h1/h2
- [x] #screen-lobby h1 increased to 40px
